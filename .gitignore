# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# MLflow (tracking DB + local artifact store are regenerated by `python src/train.py`)
mlruns/
mlflow.db
mlartifacts/

# DVC-tracked data (fetched via `dvc pull` / `dvc repro`, not committed directly)
/data/raw/breast_cancer.csv

# Model outputs
models/*.joblib
models/*.pkl

# OS / editor
.DS_Store
.idea/
.vscode/

# Env
.env
